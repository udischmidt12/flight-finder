venv/
__pycache__/
*.pyc
config.json
expenses.json
skyscanner_airports.json
prearrival_cache.json

# Secrets — every .env variant (.env, .env.local, .env.bak, ...)
.env
.env.*
*.env

# Common credential file shapes, so an auto-commit can never sweep them up
*.pem
*.key
id_rsa*
secrets.json
service-account*.json
